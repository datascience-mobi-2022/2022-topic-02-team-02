---
title: "Biomart Biotypes"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r, echo=FALSE}
library("biomartr")
```

```{r, echo=FALSE}
genesets_genes= genesets$genesets
geneset_matrix = as.matrix(genesets_genes)
rownames(geneset_matrix)
gene_names = as.matrix(noquote(geneset_matrix[,1]))
is.na(gene_names)
gene_names_noquotesapply(gene_names,noquote)
#Wang meinte in der präsentation wir sollen uns erst den biotyp von unserem geneset anschauen, vielleicht sind da auch miRNAs oder irgendwie sowas drin. die letzte variable sind die genenamen ohne anführungszeichen, irgendwie ist die geneset liste bisschen komisch idk, vielleicht können wir das so leichter in biomart einfügen
```
